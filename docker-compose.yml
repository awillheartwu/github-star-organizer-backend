version: '3.9'

services:
  api:
    build:
      context: .
    env_file:
      - .env.production
    ports:
      - "3030:3000"
    restart: unless-stopped
    volumes:
      - /volume2/docker/gsor/logs:/app/logs
    environment:
      NODE_ENV: production
    command: ["node", "dist/main.js"]
    init: true
